# Development environment variables for docker-compose.yml
# Copy to .env and adjust if needed. Defaults work out of the box.

POSTGRES_USER=pivot
POSTGRES_PASSWORD=pivot_password
POSTGRES_DB=pivot_db

# CORS origin for the backend (default: http://localhost)
CORS_ORIGIN=http://localhost
